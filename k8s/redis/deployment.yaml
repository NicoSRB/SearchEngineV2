apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: searchengine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7.0
          ports:
            - containerPort: 6379
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-credentials
                  key: password
            - name: REDISUSER 
              valueFrom:
                secretKeyRef: 
                  name: redis-credentials
                  key: user

