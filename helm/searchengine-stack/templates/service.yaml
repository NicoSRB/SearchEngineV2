apiVersion: v1
kind: Service
metadata:
  name: {{ include "searchengine-stack.fullname" . }}
  labels:
    {{- include "searchengine-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "searchengine-stack.selectorLabels" . | nindent 4 }}
