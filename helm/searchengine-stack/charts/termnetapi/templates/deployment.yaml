apiVersion: apps/v1
kind: Deployment
metadata:
  name: termnet-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: termnet-api
  template:
    metadata:
      labels:
        app: termnet-api
    spec:
      containers:
        - name: termnet-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
